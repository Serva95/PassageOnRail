<p id="notice"><%= notice %></p>

<div class="col-12 col-12-narrower">
<h2>
    Cerca la tratta
</h2>
</div>

<!--td><div class="col-6 col-12-narrower">
<form action="/search" method="/get" >
  <input type="text" name="citta_partenza">
  <input type="text" name="citta_arrivo">
  <input type="submit" value="Cerca tratta">
</form>
</div></td-->

<div class="col-6 col-12-narrower">
  <%= form_tag search_path, :method => 'get' do  %>
    <%= label_tag :'citta di partenza' %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Cerca tratta" %>
    <%end %>
</div>

<div class="col-12 col-12-narrower">
<% if !@result.any? %>
  Oh no! Sembra che nessuno percorra questa tratta :(
<% end %>
</div>

<div>
<table>
  <thead>
  <tr>
    <th>Citta partenza</th>
    <th>Luogo ritrovo</th>
    <th>Data ora partenza</th>
    <th>Citta arrivo</th>
    <th>Data ora arrivo</th>
    <th>Costo</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @result.each do |route| %>
    <tr>
      <td><%= route.citta_partenza %></td>
      <td><%= route.luogo_ritrovo %></td>
      <td><%= route.data_ora_partenza %></td>
      <td><%= route.citta_arrivo %></td>
      <td><%= route.data_ora_arrivo %></td>
      <td><%= route.costo %></td>
      <!--td><--%= link_to 'Show', route %></td-->
      <!--<td>%= link_to 'Edit', edit_route_path(route) %</td>-->
      <!--td%= link_to 'Destroy', route, method: :delete, data: { confirm: 'Are you sure?' } %/td>-->
    </tr>
  <% end %>
  </tbody>
</table>
</div>