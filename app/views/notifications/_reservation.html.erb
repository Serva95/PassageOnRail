<!-- notifica che manda il driver sulla pagina di gestione della richiesta
in questo caso il target è la route e il second target è la journey-->
<%= link_to edit_route_journey_path(notification.target.id, notification.second_target), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
  <%= notification.actor.username %>
  vuole partecipare alla tua tratta <%= notification.target.citta_partenza %> - <%= notification.target.citta_arrivo %> !
<% end %>