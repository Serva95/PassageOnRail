<!-- se viene accettata la tratta dal driver, si manda l'utente al dettaglio della prenotazione-->
<%= link_to user_detail_booking_path(notification.user, notification.target.id), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
  <%= notification.actor.username %>
  ha accettato di viaggiare con te sulla tratta <%= notification.target.citta_partenza %> - <%= notification.target.citta_arrivo %> !
<% end %>