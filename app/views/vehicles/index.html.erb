<p id="notice"><%= notice %></p>

<h1>Vehicles</h1>

<table>
  <thead>
    <tr>
      <th>Targa</th>
      <th>Marca</th>
      <th>Modello</th>
      <th>Immatricolazione</th>
      <th>Comfort</th>
      <th>Posti</th>
      <th>Deleted</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.targa %></td>
        <td><%= vehicle.marca %></td>
        <td><%= vehicle.modello %></td>
        <td><%= vehicle.immatricolazione %></td>
        <td><%= vehicle.comfort %></td>
        <td><%= vehicle.posti %></td>
        <td><%= vehicle.deleted %></td>
        <td><%= link_to 'Show', vehicle %></td>
        <td><%= link_to 'Edit', edit_driver_vehicle_path(vehicle) %></td>
        <td><%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vehicle', new_driver_vehicle_path %>
